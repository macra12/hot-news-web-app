// Re-export the useTheme hook so consumers can import from @/hooks/useTheme
// without knowing which file contains the ThemeProvider context.
export { useTheme } from "@/app/components/ThemeProvider";
